{% extends 'base.html.twig' %}

{% block body %}
    <h1>Asignar</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asignar.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('asignar_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('asignar_edit', { 'id': asignar.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
